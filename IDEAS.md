# ダッシュボード改善案（未実装分）

検討はしたが、まだ実装していないダッシュボードの改善案をここに残す。
評価軸は既存の3つ（README参照）: **リスク管理／検証と実運用の一致／見逃し防止**。

実装済みの案（ドローダウン表示・期間サマリー・CSVダウンロード・レジーム閾値の
距離表示）は [README.md](README.md) の「ブラウザで運用状況を見る」節を参照。

## 優先順位（高い順）

| 順位 | 案 | 優先度 |
|---|---|---|
| 1 | エラー・データ欠損の発生頻度トレンド | 中 |
| 2 | 累計コスト（手数料+スリッページ）の可視化 | 中〜低 |
| 3 | バックテスト実績との比較表示 | 保留（時期尚早） |
| 4 | レジーム切替履歴ログ | 低 |
| 5 | ブラウザ通知（売買発生・レジーム変化時） | 低 |

## エラー・データ欠損の発生頻度トレンド

**内容**: 直近5件のエラーだけでなく、週次・月次の発生件数をミニグラフやカウンタで出す。

**優先度**: 中。

**理由**: 1〜2回の通信断は無視してよいノイズだが、発生頻度が徐々に増えているのは
配信元や実行環境が劣化しているサイン（見逃し防止に効く）。ただし現状はエラーが
稀（そう設計されている）なため、価値が出るのは運用が長期化してからになる。

## 累計コスト（手数料+スリッページ）の可視化

**内容**: 取引成績カードに「支払った手数料・スリッページの累計」を追加する。

**優先度**: 中〜低。

**理由**: README は「手数料・スリッページを外した数字は見ない」という検証ルールを
繰り返し強調しており、実運用側でも可視化するのは一貫性がある。ただし
[broker.py](broker.py) はコストを約定価格に織り込んで記録しており、理論値
（コスト無しの価格）を別途保持していない。正確に出すには**売買ログの記録
フォーマット自体の拡張が必要**で、見た目より実装コストが高い。加えて実装時点
（2026-08-06）では取引実績がまだ乏しく、効果が出るのは売買が積み上がってから。

## バックテスト実績との比較表示

**内容**: いまの実運用の年率換算リターンや最大DDを、README記載のバックテスト
期待値（例: レジーム切替戦略 Calmar 1.58〜2.57、最大DD -26%台）と並べて
「想定内か・乖離しているか」を表示する。

**優先度**: 保留（現時点では非推奨）。

**理由**: 発想自体は「検証と実運用の一致」という軸に一番刺さるが、実運用の
サンプル数がまだ極端に少ない状態でこれをやると、単なるノイズを異常判定して
しまう。これは「過剰最適化を防ぐためのルール」（少数サンプルで動かない）と
矛盾する。**数週間〜数ヶ月分のログが溜まってから着手すること。**

## レジーム切替履歴ログ

**内容**: 「いつ・どのレジームからどのレジームに切り替わったか」を時系列
リストで表示するカード。

**優先度**: 低（2回の検討で継続して見送り）。

**理由**: 資産推移グラフ下のレジーム帯で視覚的には既に代替できており、
切替頻度自体も年間数十回程度と少ないため、限界効用が小さい。

## ブラウザ通知（売買発生・レジーム変化時）

**内容**: Web Notifications API で、タブを開いている間に売買が発生した・
レジームが変わった・ストップに接近したタイミングで通知を出す。

**優先度**: 低。

**理由**: 新規建て玉が1日1回・レジーム変化も稀という頻度の低さに対し、
実装（通知許可UI・権限管理）の割に効果が薄い。既存の15秒自動更新と
「次の売買判断まで」カウントダウンである程度代替できている。GitHub Pages
は静的配信のため、タブを開いている間だけ有効という制約もある
（真のプッシュ通知にはサーバー側の仕組みが別途必要）。
