# 改善案（未実装分）

検討はしたが、まだ実装していない改善案をここに残す。評価軸は既存の3つ
（README参照）: **リスク管理／検証と実運用の一致／見逃し防止**。
過剰最適化を防ぐためのルール（README「過剰最適化を防ぐためのルール」節）は
すべての案に共通して適用する。

実装済みの案（ドローダウン表示・期間サマリー・CSVダウンロード・レジーム閾値の
距離表示）は [README.md](README.md) の「ブラウザで運用状況を見る」節を参照。

## A. ダッシュボード/UI

| No. | 案 | 優先度 | 理由 |
|---|---|---|---|
| A1 | 建玉ごとの「ストップまでの距離」バー | 中 | 監視ユニバースの「点灯まで」と同じ表現に揃える。実装コスト低 |
| A2 | 建玉率（エクスポージャー）の推移ミニグラフ | 中 | 現状は現在値のみ。上限まで攻めていた時期が時系列で見えるとリスク管理に効く |
| A3 | 累計コスト（手数料+スリッページ）の可視化 | 中〜低 | 「手数料・スリッページを外した数字は見ない」という検証ルールとの一貫性はあるが、`broker.py` はコストを約定価格に織り込んで記録しており理論値を保持していないため**売買ログの拡張が必要**（見た目より実装コストが高い）。取引実績が乏しいうちは効果も薄い |
| A4 | バックテスト実績との比較表示 | 保留（時期尚早） | 「検証と実運用の一致」に一番刺さる案だが、実運用のサンプル数が少ない状態でやるとノイズを異常判定してしまい、過剰最適化を防ぐルール（少数サンプルで動かない）と矛盾する。**数週間〜数ヶ月分のログが溜まってから着手** |
| A5 | レジーム切替履歴ログ | 低 | 資産推移グラフ下のレジーム帯で視覚的に代替できており、切替頻度自体も年数十回と少ない（2回の検討で継続して見送り） |
| A6 | ブラウザ通知（売買発生・レジーム変化時） | 低 | 新規建て玉1日1回・レジーム変化も稀という頻度に対し実装コストが見合わない。GitHub Pagesは静的配信のためタブを開いている間だけ有効という制約もある |
| A7 | 資産推移チャートの対数軸切替 | 低 | `plot.py` のバックテスト図は既にログ軸（README記載）。複利が効き始めてから価値が出る |
| A8 | ダークモード手動トグル | 低 | 現状はOS設定追従のみ。好みの問題 |
| A9 | PWA化（直近スナップショットのオフラインキャッシュ） | 低 | 圏外でも最後の状態を見られる。実装コストはそれなり |

## B. データ・信頼性

| No. | 案 | 優先度 | 理由 |
|---|---|---|---|
| B1 | エラー・データ欠損の発生頻度トレンド | 中 | 直近5件だけでなく週次・月次の発生件数を出す。頻度増加は配信元・実行環境の劣化サイン（見逃し防止）。ただし現状エラーは稀で、価値が出るのは運用長期化後 |
| B2 | 配信元障害時の警告強化 | 中 | 「日足データが欠けています」の原因（配信元障害か取得失敗か）を切り分けて出す |
| B3 | 状態ファイルの世代バックアップ | 中 | 現状 `state/live_state.json` が壊れると元手からやり直すしかない |
| B4 | cronの発火漏れの外部監視 | 中 | README に既に書かれている未着手のTODO（外部からworkflow_dispatchを叩く） |
| B5 | GitHub Actions失敗の能動通知 | 低〜中 | 現状は見に行かないと気づけない。失敗が続いたときだけ知らせる仕組み |

## C. 戦略・リスク管理（過剰最適化ルール厳守が前提）

| No. | 案 | 優先度 | 理由 |
|---|---|---|---|
| C1 | シャドーモードでの別パラメータ並走 | 中 | 実売買には使わず判定だけ記録。「両期間の結果を見てから判断する」ルールに沿う |
| C2 | 保有期間分布の可視化 | 中 | ホイップソー傾向の検出。クールダウン分析（22.9%が翌日以内に買い直し）の延長 |
| C3 | sensitivity.py の定期回帰チェック | 中 | パラメータの平坦域が崩れていないかをCIで定期確認。過剰最適化を防ぐルール自体の自動化 |
| C4 | 観測専用のユニバース拡張（実売買はしない） | 低 | 実売買対象への安易な拡張は過剰最適化の温床になりやすい。観測限定が条件 |

## D. 自動化・運用

| No. | 案 | 優先度 | 理由 |
|---|---|---|---|
| D1 | CSVの定期自動アーカイブ | 中 | 手動ダウンロード機能の延長。週次でGitHub Releases等へ自動アップロードし、バックアップ漏れを防ぐ |
| D2 | 設定値変更の機械可読な履歴 | 低 | `regime.py` の定数変更の日付・理由をJSON等でも残す |
| D3 | 手動実行導線の整理 | 低 | スマホから `workflow_dispatch` を叩く簡単な方法をREADMEに追記 |

## E. テスト・開発体験

| No. | 案 | 優先度 | 理由 |
|---|---|---|---|
| E1 | 新機能へのテスト追加 | **高** | CSVダウンロード・レジーム閾値距離・ドローダウン計算が `test_all.py` 未収録 |
| E2 | dashboard.py APIのスキーマ検証テスト | 中 | `/api/state` の形が壊れていないかを機械的に確認 |
| E3 | エラー注入テスト | 低〜中 | 通信断・データ欠損・状態ファイル破損からの復旧を網羅的に検証 |
| E4 | dashboard.html のビジュアルリグレッションテスト | 低 | スクリーンショット比較で意図しないCSS崩れを検知 |
